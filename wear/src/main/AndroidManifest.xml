<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webonastick.ledwatch">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Required to act as a custom watch face. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- Required for complications to receive complication data and open the provider chooser. -->
    <uses-permission android:name="com.google.android.wearable.permission.RECEIVE_COMPLICATION_DATA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <service
            android:name=".LEDWatchFace"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/preview_digital" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview_circular"
                android:resource="@drawable/preview_digital_circular" />

            <!-- https://developer.android.com/training/wearables/watch-faces/configuration -->
            <!-- Specify an intent for configuration activities. -->
            <!--
            <meta-data
                android:name="com.google.android.wearable.watchface.wearableConfigurationAction"
                android:value="com.webonastick.ledwatch.LEDWatchFace.CONFIG_DIGITAL" />
            -->

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />

                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- https://developer.android.com/training/wearables/watch-faces/configuration -->
        <!-- Create a wearable configuration activity. -->
        <!--
        <activity
            android:name=".LEDWatchFaceWearableConfigActivity"
            android:label="@string/digital_config_name">
            <intent-filter>
                <action android:name="com.webonastick.ledwatch.LEDWatchFace.CONFIG_DIGITAL" />
                <category android:name="com.google.android.wearable.watchface.category.WEARABLE_CONFIGURATION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        -->

        <!--
        In your configuration activity, build a UI that provides options to customize all the configurable elements of your watch face. When users make a selection, use the Wearable Data Layer API to communicate the configuration change to the watch face activity.
        https://developer.android.com/training/wearables/data-layer/index.html

        For more details, see the DigitalWatchFaceCompanionConfigActivity class in the WatchFace sample.
        https://developer.android.com/samples/WatchFace/index.html
        -->

    </application>

</manifest>